mutation createIngredient(
  $amount: String
  $ingredient: String!
  $measure: ID!
  $recipe: ID!
) {
  createIngredient(
    input: {
      amount: $amount
      ingredient: $ingredient
      measure: $measure
      recipe: $recipe
    }
  ) {
    data {
      id
    }
  }
}
